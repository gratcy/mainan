  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <h1>
        City
        <small>City</small>
      </h1>
      <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li class="active"><a href="#">City</a></li>
      </ol>
    </section>

    <!-- Main content -->
    <section class="content">
      <div class="row">
        <div class="col-xs-12">
          <div class="box">
            <div class="box-header">
              <h3 class="box-title">City</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
            {{{ error_msg }}}
            
            <div class="box-header">
			{{#if (__check_permission 'CityExecute') }}
		    <h3 class="box-title">
			<a href="{{ __site_url 'city/city_add' }}" class="btn btn-default"><i class="fa fa-plus"></i> Add City</a>
			</h3>
            {{/if}}
            </div>
              <table id="sTable" class="table table-bordered table-striped">
                <thead>
                <tr>
                  <th>Province</th>
                  <th>City</th>
                  <th>Status</th>
                  <th>Action</th>
                </tr>
                </thead>
                <tbody>
				{{#each data}}
                <tr>
                  <td>{{ pname }}</td>
                  <td>{{ cname }}</td>
                  <td>{{ __get_status cstatus 1 }}</td>
                  <td>
					{{#if (__check_permission 'CityExecute') }}
					<a href="{{ __site_url 'city/city_update/' }}{{ cid }}"><i class="fa fa-pencil"></i></a>
					<a href="{{ __site_url 'city/city_delete/' }}{{ cid }}" onclick="return confirm('Are you sure you want to delete this item?');"><i class="fa fa-times"></i></a>
					{{/if}}
                  </td>
                </tr>
                {{/each}}
                </tbody>
                <tfoot>
                <tr>
                  <th>Province</th>
                  <th>City</th>
                  <th>Status</th>
                  <th>Action</th>
                </tr>
                </tfoot>
              </table>
            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
        </div>
        <!-- /.col -->
      </div>
      <!-- /.row -->
    </section>
    <!-- /.content -->
</div>
